<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Form</title>
</head>
<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js" integrity="sha512-894YE6QWD5I59HgZOGReFYm4dnWc1Qt5NtvYSaNcOP+u1T9qYdvdihz0PPSiiqn/+/3e7Jo4EaG7TubfWGUrMQ==" crossorigin="anonymous" referrerpolicy="no-referrer"></script>

<body>
  <h2>Get 요청</h2>
  <form action="/login" method="get">
    <label for="id">ID</label>
    <input type="text" name="id" id="id">
    <label for="pw">PW</label>
    <input type="password" name="pw" id="pw">

    <input type="text" name="email">
    <input type="text" name="grade">

    <button>제출</button>
  </form>

  <h2>Post 요청</h2>
  <form action="/login" method="post">
    <label for="id">ID</label>
    <input type="text" name="id" id="id" required placeholder="ID" pattern="^([a-zA-Z0-9가-힣]){4,}$" title="대소문자, 숫자, 한글, 4글자 이상">
    <label for="pw">PW</label>
    <input type="password" name="pw" id="pw" required placeholder="PW" pattern="^([a-z0-9]){8,12}$" title="소문자, 숫자, 8~12자">

    <input type="text" name="email">
    <input type="text" name="grade">

    <button>제출</button>
  </form>

  <hr>


  <!-- js form validation -->
  <!-- onsubmit에 false 리턴되면 서버로 넘어가지 않음 -->
  <form action="/js-form-check" , method="post" onsubmit="return checkForm(this)">
    <input type="text" name="id" placeholder="ID" required minlength="4" title="최소 4 글자">
    <span>우편물 수령 장소</span>
    <input type="checkbox" name="post" value="자택" checked />자택
    <input type="checkbox" name="post" value="회사" />회사
    <button>제출</button>
  </form>
  <script>
    function checkForm(form) {
      if (form.id.value == "") {
        form.id.focus();
        alert("아이디를 입력해주세요");
        return false;
      }

      if (form.post[0].checked == false && form.post[1].checked == false) {
        alert("우편물 수령 장소를 선택해주세요!");
        return false;
      }
    }
  </script>

  <hr><br><br><br>
  <form action="/practice" method="post">
    <label for="name">이름</label>
    <input type="text" name="name" required>
    <br><br>
    <fieldset>
      <legend>성별</legend>
      <input type="radio" name="gender" id="male" value="남자" required>
      <label for="male">남자</label>
      <input type="radio" name="gender" id="female" value="여자" required>
      <label for="female">여자</label>
    </fieldset>
    <fieldset>
      <legend>생년월일</legend>
      <select name="birthday" id="year">
        <% for( let i = 0; i <= 80; i++ ) { %>
        <option value="<%= 2024 - i %>"><%= 2024 - i %></option>
        <% } %>
      </select>년
      <select name="birthday" id="month">
        <% for( let i = 1; i <= 12; i++ ) { %>
        <option value="<%= i %>"><%= i %></option>
        <% } %>
      </select>월
      <select name="birthday" id="day">
        <% for( let i = 1; i <= 31; i++ ) { %>
        <option value="<%= i %>"><%= i %></option>
        <% } %>
      </select>일
    </fieldset>

    <fieldset>
      <legend>관심사</legend>
      <input type="checkbox" name="interest" value="여행">여행
      <input type="checkbox" name="interest" value="패션">패션
      <input type="checkbox" name="interest" value="음식">음식
    </fieldset>

    <fieldset>
      <legend>선택</legend>
      <p>좋아하는 색상 <input type="color" name="color"></p>
      <p>좋아하는 숫자 <input type="number" name="number"></p>

    </fieldset>
    <br>
    <button>회원가입</button>
  </form>
</body>

</html>